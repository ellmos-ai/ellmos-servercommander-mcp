# Original Artwork

This folder keeps source-resolution artwork for future reuse.

- `servercommander-logo-original.png`: original generated ServerCommander MCP logo supplied by the project owner.

The public README uses the optimized asset at `assets/servercommander-logo.jpg` to keep GitHub and npm loading fast. This `artwork/` folder is intentionally not included in the npm package file list.

# Original Artwork

This folder keeps source-resolution artwork for future reuse.

- `servercommander-logo-original.png`: original generated ServerCommander MCP logo supplied by the project owner.

The public READMEs use the central ellmos emblem hosted in the `ellmos-ai/.github` repo; the optimized per-project logo asset was retired with that change. This `artwork/` folder is intentionally not included in the npm package file list.
